version: 2

before:
  hooks:
    - go mod download
builds:
  - env:
      - CGO_ENABLED=0
    goos:
      - darwin
      - linux
      - windows
    ldflags:
      - "-s"
      - "-w"
      - "-X github.com/fmotalleb/go-tools/git.Version={{.Version}}"
      - "-X github.com/fmotalleb/go-tools/git.Commit={{.ShortCommit}}"
      - "-X github.com/fmotalleb/go-tools/git.Date={{.Date}}"
      - "-X github.com/fmotalleb/go-tools/git.Branch={{.Branch}}"
    goarch:
      - amd64
      - arm64


dockers_v2:
  - id: edged
    sbom: false
    platforms:
      - linux/amd64
      - linux/arm64
    images:
      - ghcr.io/fmotalleb/edged
    tags:
      - "ghcr.io/fmotalleb/edged:latest"
      - "ghcr.io/fmotalleb/edged:{{ .Major }}"
      - "ghcr.io/fmotalleb/edged:{{ .Major }}.{{ .Minor }}"
      - "ghcr.io/fmotalleb/edged:{{ .Major }}.{{ .Minor }}.{{ .Patch }}"
    labels:
      "org.opencontainers.image.description": "Junction"
      "org.opencontainers.image.created": "{{.Date}}"
      "org.opencontainers.image.name": "{{.ProjectName}}"
      "org.opencontainers.image.revision": "{{.FullCommit}}"
      "org.opencontainers.image.version": "{{.Version}}"
      "org.opencontainers.image.source": "{{.GitURL}}"
    ids:
      - default

release:
  github:
  name_template: "{{.Version}}"
  prerelease: auto
  mode: append
changelog:
  disable: true
